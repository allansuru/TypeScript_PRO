enum Sizes {
    Small,
    Medium,
    Large,
}

enum Sizes {
    ExtraLarge = 3
}

console.log(Sizes.Large, Sizes[Sizes.Large]);


console.log(Sizes);

const selectedSize = 3

console.log(Sizes[selectedSize]);

